upstream test-app {
    server eatda-test-green:8080;
}

server {
    listen 80;
    
    client_max_body_size 10M;

    location / {
        proxy_pass http://test-app;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
