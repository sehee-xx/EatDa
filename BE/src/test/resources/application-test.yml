spring:
  datasource:
    url: jdbc:mysql://localhost:3306/eatda?serverTimezone=Asia/Seoul
    username: root
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    com.domain.review: DEBUG
    org.springframework.transaction: DEBUG
