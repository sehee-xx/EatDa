spring:
  datasource:
    url: jdbc:mysql://localhost:3306/eatda?serverTimezone=Asia/Seoul
    username: root # 변경필요
    password: your_password # 변경필요
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true


#mysql 용
